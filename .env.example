# ===========================================
# RIZBOT IDX - Environment Variables
# ===========================================
# 
# ⚠️ RECOMMENDED: Masukkan RIZBOT AI Key via aplikasi web
#    Menu: Settings > API Key Settings
#    Ini lebih aman karena key tidak di-bundle ke kode
#
# ===========================================

# RIZBOT AI Key
# Dapatkan API key di: https://makersuite.google.com/app/apikey
# 
# OPSI 1 (Recommended): Masukkan via web interface
#   - Buka aplikasi > Login > Settings > API Key Settings
#   - Key disimpan di localStorage browser Anda
#
# OPSI 2 (Development only): Uncomment baris di bawah
# VITE_GEMINI_API_KEY=your_api_key_here

# Application Settings (optional)
VITE_APP_NAME=RIZBOT IDX
VITE_APP_VERSION=1.0.0

# ===========================================
# SECURITY NOTES
# ===========================================
# 
# ⚠️ IMPORTANT: This application stores the API key in client-side code.
# For production deployment, you should:
#
# 1. Create a backend server to proxy AI requests
# 2. Store API keys securely on the server (never in frontend)
# 3. Use environment variables on your server
# 4. Implement proper authentication between frontend and backend
#
# Example backend setup (Node.js/Express):
# - Create an API endpoint: POST /api/ai/analyze
# - Store API key in server environment
# - Frontend calls your backend, backend calls AI service
# - This way the API key is never exposed to users
#
# ===========================================
